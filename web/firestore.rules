rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEMO: production'da koleksiyon bazlı sıkı kurallar tanımlayın
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
